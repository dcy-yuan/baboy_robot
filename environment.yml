name: cv_gesture
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - numpy>=1.24.0
  - pyyaml>=6.0
  - pip:
    - opencv-python>=4.8.0
    - mediapipe>=0.10.0
    - pyserial>=3.5
